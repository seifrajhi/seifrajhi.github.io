---
const {date} = Astro.props;
---
<time class="created-at" datetime={date.toISOString()}>
    {date.toLocaleDateString("en-US", {month: "short", day: "numeric", year: "numeric"})}
</time>

<style>
    .created-at {
        font-weight: 600;
        text-transform: uppercase;
    }
</style>